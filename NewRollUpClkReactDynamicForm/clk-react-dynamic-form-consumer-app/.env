EXTEND_ESLINT=true
# OR if the above doesn't work, use this to at least get the app running:
DISABLE_ESLINT_PLUGIN=true
# Used only to override relative paths for ex `await fetch('/backend/v1` `await fetch('https://c3edu.online/backend/v1`
REACT_APP_OVERRIDE_FETCH_PATH_URL=https://c3edu.online/backend/v1
